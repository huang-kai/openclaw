# Skills

Custom skills for OpenClaw assistant.

## Structure

Each skill should be in its own subdirectory with a `SKILL.md` file.

## Usage

Copy or clone skills to `~/.openclaw/skills/` directory.

